# Simple Shop API

Асинхронный REST API для простого интернет-магазина, построенный на FastAPI.
Проект включает регистрацию пользователей, управление товарами и заказами, кэширование и асинхронную обработку событий через очередь сообщений.

## Стек технологий

- **Python 3.10+**
- **FastAPI**: Современный веб-фреймворк.
- **PostgreSQL**: Основная база данных (драйвер `asyncpg`).
- **SQLAlchemy 2.0**: ORM для работы с БД.
- **Alembic**: Управление миграциями БД.
- **Redis**: Кэширование запросов (товары, списки заказов).
- **RabbitMQ**: Брокер сообщений (события регистрации и создания заказов).
- **Docker & Docker Compose**: Контейнеризация приложения и окружения.
- **Pytest**: Тестирование (Unit и Integration тесты).

## 🚀 Запуск проекта

### Предварительные требования
У вас должны быть установлены [Docker](https://www.docker.com/) и [Docker Compose](https://docs.docker.com/compose/install/).

### Быстрый старт

1. **Клонируйте репозиторий:**
   ```bash
   git clone <your-repo-url>
   cd simple-shop-api
   ```

2. **Запустите приложение:**
  ```bash
  docker-compose up --build -d
  ```

Эта команда соберет образы и запустит следующие контейнеры:
   * `db`: PostgreSQL база данных.
   * `redis`: Сервис кэширования.
   * `rabbitmq`: Брокер сообщений.
   * `app`: Основное API приложение (доступно на порту 8000).
   * `worker`: Фоновый обработчик событий из RabbitMQ.
   * `alembic_migrator`: Временный контейнер для применения миграций БД.

3. **Документация API:**
   Откройте браузер по адресу [http://localhost:8000/docs](http://localhost:8000/docs), чтобы увидеть интерактивную документацию Swagger UI. 
   Альтернативная документация доступна по адресу [http://localhost:8000/redoc](http://localhost:8000/redoc).

## Миграции базы данных

Миграции применяются автоматически при старте контейнеров благодаря сервису `alembic_migrator`.

Если вы внесли изменения в модели (`app/models.py`) и хотите создать новую миграцию:

1. Убедитесь, что контейнеры запущены.
2. Выполните команду генерации миграции (например, внутри контейнера `app`):
  ```bash
  docker-compose exec -w /app/app app alembic revision --autogenerate -m "описание изменений"
  ```
  
3. Примените миграции (если не хотите перезапускать контейнеры):
  ```bash
  docker-compose exec -w /app/app  app alembic upgrade head
  ```
  
## Тестирование

Проект покрыт юнит-тестами и интеграционными тестами. Для тестов используется `pytest` с моками для RabbitMQ и Redis, а также in-memory база данных SQLite, поэтому они выполняются быстро и изолированно.

**Запуск тестов:**
  ```bash
  # Запуск тестов внутри Docker-контейнера (рекомендуемый способ)
  docker-compose exec app pytest -v
  
  # Запуск локально
  pytest -v
  ```

## 🔌 Примеры запросов (API)

Ниже приведены примеры запросов с использованием `curl`.

### 1. Регистрация пользователя
  ```bash
  curl -X 'POST' \
    'http://localhost:8000/api/register' \
    -H 'Content-Type: application/json' \
    -d '{
    "email": "user@example.com",
    "password": "strongpassword"
  }'
  ```

### 2. Авторизация (Получение токена)
  ```bash
  curl -X 'POST' \
    'http://localhost:8000/api/login' \
    -H 'Content-Type: application/x-www-form-urlencoded' \
    -d 'username=user@example.com&password=strongpassword'
```
*В ответе вы получите `access_token`. Используйте его в заголовке `Authorization: Bearer <token>` для защищенных эндпоинтов.*

### 3. Создание товара (Требует авторизации)
  ```bash
  curl -X 'POST' \
    'http://localhost:8000/api/products' \
    -H 'Authorization: Bearer <YOUR_ACCESS_TOKEN>' \
    -H 'Content-Type: application/json' \
    -d '{
    "name": "Gaming Laptop",
    "price": 1500.0
  }'
  ```
  
### 4. Создание заказа (Асинхронно отправляет событие)
  ```bash
  curl -X 'POST' \
    'http://localhost:8000/api/orders' \
    -H 'Authorization: Bearer <YOUR_ACCESS_TOKEN>' \
    -H 'Content-Type: application/json' \
    -d '{
    "product_id": 1,
    "quantity": 1
  }'
  ```
